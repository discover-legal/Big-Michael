# Big Michael — Python dependencies for PDF/OCR tools
# Install: pip install -r requirements.txt
#
# System requirements:
#   OCR:    apt install tesseract-ocr   OR  brew install tesseract
#           Additional lang packs: apt install tesseract-ocr-fra tesseract-ocr-deu etc.
#   Tables: apt install ghostscript     OR  brew install ghostscript
#           Then use: pip install "camelot-py[cv]"  (replaces [base] for lattice mode)

PyMuPDF>=1.23.0
camelot-py[base]>=0.11.0
pytesseract>=0.3.10
Pillow>=10.0.0
